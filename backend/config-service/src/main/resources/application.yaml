server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVICE

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations:
            - ${CONFIG_REPO_PATH:file:../config-repo}

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
