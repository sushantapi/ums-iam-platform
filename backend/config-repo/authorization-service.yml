server:
  port: 8083

spring:
  application:
    name: AUTHORIZATION-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/authz_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  public-key-path: keys/public_key.pem