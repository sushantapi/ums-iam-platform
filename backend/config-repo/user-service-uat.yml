server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://uat-mysql:3306/user_db
    username: user_uat
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  public-key-path: keys/public.pem