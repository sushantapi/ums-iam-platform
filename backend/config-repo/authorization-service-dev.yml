server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authz_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  public-key-path: keys/public_key.pem